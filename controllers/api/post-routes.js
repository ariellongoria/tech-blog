const router = require("express").Router();
const { route } = require(".");
const { Post, User, Vote, Comment } = require("../../models");

router.get();
router.get();
router.post();
router.put();
router.delete();
